工作任务
============================

.. toctree::
   :maxdepth: 2

这里记录一些工作任务或备忘。



工作相关的任务备忘
^^^^^^^^^^^^^^^^^^^^^^
- 了解压缩算法：如果不知道数据的逻辑，压缩算法是压缩不了多少。
- 将手上的任务，全部在这里列出来，并安排优先级。

- 对于现在的ES，我会用时间换“可靠性”，我要在三个月内将ES整的得心应手。

  - 搜索的索引走相同的架构。
  - 索引的字段统一。

    - 字段名不出现1、2、3等数字。
    - 不可分词的field的名称统一成 `keyword` ，修正 `not` 的情况。
    - category字段统一为数组
    - 索引名都改为 `import` 不要在用 `build` 了。
    - 营销案例中，有很多不必要的字段设置了keyword。

  - 索引的测试架构。
  - EsBehavoir对于新功能的上线周期变短。就像年前新渠道的上线，由两天变为不到一个小时。

- 查询问题：ES磁盘扩容后的大小不对。
- 学习 ``kibana`` 。

计划
^^^^^^^^^^^^^
- 我的开源经历就从翻译开始吧！

  - Spring Boot Cloud Config

为了再就业
^^^^^^^^^^^^^
- 了解tablestore用法。
